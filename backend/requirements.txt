# --extra-index-url 告知 pip 除了預設的 PyPI 外，也到這個網址尋找套件
--extra-index-url https://download.pytorch.org/whl/cu126

# PyTorch 相關套件
torch==2.9.0
torchvision
torchaudio

# Web 框架與伺服器
fastapi
uvicorn[standard]
python-multipart

# Hugging Face 生態系
transformers>=4.41.0
accelerate>=0.31.0
optimum>=1.20.0
huggingface_hub

# 語者分離套件
pyannote.audio

# Whisper 轉錄
faster-whisper

# 其他依賴
numpy
python-dotenv
opencc-python-reimplemented

# 安全相關套件（SSDLC 要求）
cryptography>=41.0.0  # 加密和密碼處理
slowapi>=0.1.9  # API 速率限制
email-validator>=2.0.0  # 郵箱驗證
python-magic-bin>=0.4.14; sys_platform == 'win32'  # Windows 文件類型檢測
python-magic>=0.4.27; sys_platform != 'win32'  # Linux/Mac 文件類型檢測

bandit
pip-audit