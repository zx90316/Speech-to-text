# 波形可視化播放器功能實作

## 🎨 新設計概覽

### 現代化左右佈局

```
┌─────────────────────────────────────────────────────────┐
│                    音訊預處理介面                          │
├──────────────────┬──────────────────────────────────────┤
│                  │                                      │
│   左側：操作面板    │        右側：預覽面板                  │
│                  │                                      │
│  ┌─────────────┐ │  ┌────────────────────────────────┐ │
│  │ 檔案上傳     │ │  │  原始音訊波形                   │ │
│  └─────────────┘ │  │  [━━━━━━波形圖━━━━━━]          │ │
│                  │  │  [◄◄ ▶ ►►] 時間 音量             │ │
│  ┌─────────────┐ │  └────────────────────────────────┘ │
│  │ 快速預設     │ │                                      │
│  │ [通話清理]   │ │  ┌────────────────────────────────┐ │
│  │ [戶外錄音]   │ │  │  處理後音訊波形                 │ │
│  │ [會議錄音]   │ │  │  [━━━━━━波形圖━━━━━━]          │ │
│  └─────────────┘ │  │  [◄◄ ▶ ►►] 時間 音量             │ │
│                  │  └────────────────────────────────┘ │
│  ┌─────────────┐ │                                      │
│  │ 處理參數     │ │  ┌────────────────────────────────┐ │
│  │ ☑ 降噪       │ │  │       [下載處理後音訊]          │ │
│  │   強度 70%   │ │  └────────────────────────────────┘ │
│  │ ☑ 正規化     │ │                                      │
│  │ ☑ 人聲增強   │ │                                      │
│  │ ☐ 迴聲消除   │ │                                      │
│  └─────────────┘ │                                      │
│                  │                                      │
│ [開始預處理]      │                                      │
└──────────────────┴──────────────────────────────────────┘
```

## ✨ 核心功能

### 1. 波形可視化播放器 (WaveformPlayer)

#### 功能特色
- ✅ **實時波形繪製**：使用 Canvas API 繪製音訊波形
- ✅ **互動式進度條**：點擊波形跳轉到指定位置
- ✅ **完整播放控制**：
  - 播放/暫停
  - 快進 5 秒 / 快退 5 秒
  - 音量控制（0-100%）
  - 靜音切換
- ✅ **時間顯示**：當前時間 / 總時長
- ✅ **視覺化回饋**：播放進度高亮顯示

#### 技術實作
```typescript
interface WaveformPlayerProps {
  audioUrl: string;      // 音訊 URL
  title: string;         // 標題
  color?: string;        // 波形顏色（預設藍色）
}
```

**核心技術：**
- Web Audio API - 音訊解碼與處理
- Canvas 2D - 波形繪製
- React Hooks - 狀態管理

### 2. 現代化預處理介面

#### 左側操作面板

**檔案上傳區**
- 拖放上傳
- 點擊選擇
- 檔案資訊顯示（名稱、大小）
- 移除檔案按鈕

**快速預設方案**
- 通話清理：降噪 + 正規化 + 人聲增強 + 壓縮
- 戶外錄音：強降噪 + EQ + 人聲增強
- 會議錄音：降噪 + 迴聲消除 + 靜音移除

**處理參數控制**
- 降噪處理（強度可調）
- 音量正規化（峰值/LUFS）
- 人聲增強（強度可調）
- 迴聲消除
- 靜音移除
- 動態壓縮
- 單聲道轉換

#### 右側預覽面板

**三種狀態：**

1. **初始狀態**
   - 顯示提示圖示
   - 引導用戶上傳檔案

2. **檔案已上傳**
   - 顯示原始音訊波形播放器
   - 提示點擊預處理

3. **預處理完成**
   - 上下對比顯示：
     - 原始音訊波形（灰色）
     - 處理後音訊波形（綠色）
   - 每個波形都有獨立播放器
   - 下載按鈕

## 🎨 視覺設計

### 配色方案
- **原始音訊**：`#94a3b8` (灰色調)
- **處理後音訊**：`#10b981` (綠色調)
- **主要操作**：`#6366f1` (藍紫色)
- **成功/下載**：`#10b981` (綠色)

### 互動動畫
- 按鈕 hover 上浮效果
- 播放控制按鈕縮放
- 音量滑桿拖動反饋
- 檔案拖放高亮

## 📁 檔案結構

```
frontend/src/components/
├── WaveformPlayer.tsx       # 波形播放器組件
├── WaveformPlayer.css       # 播放器樣式
├── AudioPreprocessor.tsx    # 預處理主組件（新版）
└── AudioPreprocessor.css    # 預處理樣式（新版）
```

## 🔧 使用方式

### 基礎用法

```tsx
import { WaveformPlayer } from './components/WaveformPlayer';

<WaveformPlayer
  audioUrl="/path/to/audio.mp3"
  title="我的音訊"
  color="#6366f1"
/>
```

### 完整預處理流程

1. **上傳檔案**
   - 拖放或點擊上傳音訊檔案
   - 查看原始波形預覽

2. **選擇預設或自訂參數**
   - 點擊快速預設（通話清理/戶外/會議）
   - 或手動調整各項參數

3. **執行預處理**
   - 點擊「開始預處理」按鈕
   - 等待處理完成

4. **對比預覽**
   - 查看原始 vs 處理後波形
   - 使用播放器聆聽效果
   - A/B 對比音質

5. **下載結果**
   - 點擊下載按鈕
   - 獲取處理後的 WAV 檔案

## 🎯 特色功能

### 1. 即時波形顯示
使用 Web Audio API 解析音訊數據，繪製精確的波形圖：

```javascript
// 載入音訊
const audioContext = new AudioContext();
const buffer = await audioContext.decodeAudioData(arrayBuffer);

// 繪製波形
const data = buffer.getChannelData(0);
const step = Math.ceil(data.length / canvasWidth);
// ... 繪製邏輯
```

### 2. 互動式播放控制
- 點擊波形跳轉
- 鍵盤快捷鍵（未來可擴展）
- 觸控支援

### 3. 響應式設計
- 桌面：左右雙欄佈局
- 平板/手機：上下單欄佈局
- Canvas 自動調整尺寸

## 📊 效能優化

### Canvas 渲染優化
- 只在必要時重繪（時間更新、進度變化）
- 使用 RequestAnimationFrame
- 波形數據採樣（避免繪製過多點）

### 記憶體管理
- 音訊數據解碼後快取
- 組件卸載時釋放資源
- Object URL 適時釋放

## 🌟 未來擴展

### 計劃功能
- [ ] 多軌道波形對比
- [ ] 頻譜分析顯示
- [ ] 音訊片段標記
- [ ] 波形縮放功能
- [ ] 匯出波形圖片
- [ ] 更多預設方案
- [ ] 參數儲存/載入
- [ ] 批次預處理

### 進階功能
- [ ] 即時預覽（處理前即可聽到效果）
- [ ] AI 智能參數推薦
- [ ] 音訊品質自動評估
- [ ] 處理歷史記錄

## 🎨 設計原則

1. **直覺性**：左側操作，右側預覽，符合用戶習慣
2. **即時性**：波形即時顯示，參數即時調整
3. **可比較性**：原始與處理後並排對比
4. **專業性**：完整的播放控制，精確的時間顯示
5. **美觀性**：現代化深色主題，流暢動畫

## ✅ 完成狀態

- [x] 波形可視化播放器組件
- [x] 完整播放控制（播放、暫停、快進、快退）
- [x] 音量控制與靜音
- [x] 時間顯示與進度條
- [x] 左右分欄佈局
- [x] 檔案上傳區域
- [x] 快速預設方案
- [x] 參數控制面板
- [x] 對比預覽功能
- [x] 下載功能
- [x] 響應式設計

**實作完成日期：** 2025-10-02
